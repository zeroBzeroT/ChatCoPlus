plugins {
    // Lets Gradle fetch the Java 21 toolchain if the machine does not have one.
    id("org.gradle.toolchains.foojay-resolver-convention") version "1.0.0"
}

rootProject.name = "ChatCoPlus"
